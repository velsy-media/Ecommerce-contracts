summary: Add a product to the cart
description: Add a product to the cart of the user
tags:
  - Cart
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: "@/schemas/cart/AddToCartRequest.yaml"
responses:
  "200":
    description: Success
    content:
      application/json:
        schema:
          $ref: "@/schemas/cart/AddToCartResponse.yaml"
  "400":
    description: Error
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/ErrorResponse"
  "401":
    description: Unauthorized
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/ErrorResponse"
  "500":
    description: Internal Server Error
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/ErrorResponse"
