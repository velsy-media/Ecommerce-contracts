post:
  summary: Create a user
  description: Create a user
  tags:
    - Users
  requestBody:
    description: User to create
    required: true
    content:
      application/json:
        schema:
          $ref: "@/schemas/user/RegisterRequest.yaml"